using UnityEngine;
using UnityEngine.UI;
using static UnityEditor.Progress;

public class DecisionScript : MonoBehaviour
{
    [SerializeField] Image correctAnswer;
    [SerializeField] Image IncorrectAnswer;

    // Update is called once per frame
    void Update()
    {
    }
}
